// এটাকে "third-party class" ধরে নেও (edit করা যায় না বলে ধরে নিচ্ছি) — @Component
// বসাবা না। AppConfig-এ @Bean method দিয়ে register করতে হবে (sec 16)
public class IdGenerator {

    // private int counter = 1000;

    // nextId() — counter++ রিটার্ন করো
}
