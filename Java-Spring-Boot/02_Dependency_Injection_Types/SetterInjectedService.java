// Setter Injection — dependency একটা setter method দিয়ে পরে সেট করা যায় (object তৈরির
// পরেও বদলানো যায়, কিন্তু object partially-constructed অবস্থায় ব্যবহৃত হওয়ার risk থাকে)
public class SetterInjectedService {

    // private MessageSender sender;

    // setSender(MessageSender sender)

    // notifyUser(String message) — sender.send(message)
}
