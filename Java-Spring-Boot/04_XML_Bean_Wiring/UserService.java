// constructor-এ NotificationService নিবে — XML-এ constructor-arg ref দিয়ে inject হবে
public class UserService {

    // private NotificationService notificationService;

    // constructor(NotificationService notificationService)

    // createUser(String name) — "User " + name + " created." print করো, তারপর
    // notificationService.send("Welcome " + name + "!") call করো
}
