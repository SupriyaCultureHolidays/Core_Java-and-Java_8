// Question 5 — Annotation-Based Config (sec 10, 11)
// Question 4-এর একই কাজ, কিন্তু XML ছাড়া — নিজেই তুলনা করে দেখো কতটা কোড কমে গেলো
//
// Output:
// User Rahim created. Notification sent: Welcome Rahim!
public class Demo {
    public static void main(String[] args) {
        // 1. ApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);
        // 2. UserService userService = context.getBean(UserService.class);
        // 3. userService.createUser("Rahim");

        // Question 4 (XML)-এর সাথে code-এর পরিমাণ তুলনা করে একটা comment এখানে লিখো
    }
}
