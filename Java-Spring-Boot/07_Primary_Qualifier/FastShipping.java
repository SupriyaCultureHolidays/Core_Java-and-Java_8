// @Component বসাও। প্রথমে @Primary ছাড়া চালিয়ে ambiguous-bean error দেখো, তারপর
// এখানে @Primary বসিয়ে ঠিক করো (Approach 1 — sec 14)
public class FastShipping implements ShippingService {

    // ship() — return "Fast Shipping"
}
