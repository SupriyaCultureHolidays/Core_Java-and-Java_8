// Custom unchecked exception (Core Java Exception topic-এর practice) — id দিয়ে employee
// না পেলে EmployeeServiceImpl এটা throw করবে
public class EmployeeNotFoundException extends RuntimeException {

    // constructor(String message) — super(message) call করো
}
