// POJO — Employee entity (Core Java Encapsulation-এর মতোই: id, name, department, salary
// সব private রাখবা, constructor + getter/setter লিখবা)। এখানে কোনো Spring annotation
// লাগবে না — এটা data object, bean বানানো হয় না (প্রতিটা employee-এর জন্য আলাদা instance
// লাগে, কিন্তু Spring bean default scope singleton — data object কখনো bean বানানো হয় না)
public class Employee {

    // private int id;
    // private String name;
    // private String department;
    // private double salary;

    // সব field নেওয়া constructor লিখো

    // getter/setter — সব field-এর জন্য

    // toString() override করো — display-এ কাজে লাগবে
}
