// Custom Checked Exception — withdraw/transfer-এর সময় পর্যাপ্ত balance (বা overdraft limit) না থাকলে এটা throw করবে
public class InsufficientFundsException extends Exception {

    // constructor-এ message নাও, super(message) call করো
}
