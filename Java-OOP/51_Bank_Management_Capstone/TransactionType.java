// Enum — প্রতিটা transaction এই চারটা type-এর যেকোনো একটা হবে
public enum TransactionType {
    // DEPOSIT, WITHDRAW, TRANSFER_IN, TRANSFER_OUT — এই চারটা constant লিখো
    DEPOSIT, WITHDRAW, TRANSFER_IN, TRANSFER_OUT;

}
