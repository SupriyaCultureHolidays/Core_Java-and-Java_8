// Custom Checked Exception — course-এর capacity ভর্তি (ENROLLED status-এর count == capacity)
// থাকলে এটা throw করবে
public class CourseFullException extends Exception {

    // constructor-এ message নাও, super(message) call করো
}
