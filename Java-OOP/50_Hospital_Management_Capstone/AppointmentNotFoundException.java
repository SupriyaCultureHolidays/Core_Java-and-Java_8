// Custom Checked Exception — appointmentId দিয়ে appointment না পেলে এটা throw করবে
public class AppointmentNotFoundException extends Exception {

    // constructor-এ message নাও, super(message) call করো
}
