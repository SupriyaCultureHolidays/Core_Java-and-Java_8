public class Student extends LibraryPerson {

    // LibraryPerson-এর id, name ছাড়াও চাইলে borrowedBookIds (List<String>) রাখতে পারো
    // constructor লিখো, super(id, name) call করো

    // getRole() override করো — return "Student"
}
