/*
    TOPIC: Stream API, Optional Class

    Q1) ArrayList<Integer> banao, stream() diye forEach() use kore proti
        element print koro.

    Q2) map(), filter(), reduce() ekshathe use kore ekta list theke
        even number gulo nao, square koro, ar sob add koro.

    Q3) sorted() diye ekta list of String ke alphabetical order e sort koro
        stream diye.

    Q4) Parallel stream () kokhon use kora uchit, normal stream er sathe
        difference ki - comment e likho, ekta example diye dekhao.

    Q5) Optional class use kore null check avoid koro - ekta method banao
        jeta Optional<String> return kore, isPresent()/orElse() use koro.
*/
public class Demo {
    public static void main(String[] args) {
        // apnar answer/code ekhane likhun

    }
}
